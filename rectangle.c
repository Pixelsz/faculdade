#include <stdio.h>

int main()
{
    int length = 8;
    int width = 10;
    int area;

    area = width * length;

    printf("Length is %d\n", length);
    printf("Length is %d\n", width);
    printf("Area of the rectangle is: %d\n", area);
}